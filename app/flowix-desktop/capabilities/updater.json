{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "updater",
  "description": "Update Flowix from trusted first-party application windows",
  "windows": ["main", "preferences"],
  "permissions": [
    "updater:default",
    "process:allow-restart"
  ]
}
