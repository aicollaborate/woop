diff --git a/scripts/install-lefthook.mjs b/scripts/install-lefthook.mjs
index 0000000..0000000 100755
--- a/scripts/install-lefthook.mjs
+++ b/scripts/install-lefthook.mjs
@@ -14,6 +14,5 @@ import {
 } from 'node:fs'
 import { spawnSync } from 'node:child_process'
 import { dirname, isAbsolute, join, resolve } from 'node:path'
-import lefthookPackage from 'lefthook/package.json' with { type: 'json' }
 
 const MINIMUM_GIT = [2, 26, 0]
@@ -690,3 +690,4 @@
 async function main() {
   if (process.env.CI === 'true' || process.env.GITHUB_ACTIONS === 'true') return
+  const { default: lefthookPackage } = await import('lefthook/package.json', { with: { type: 'json' } })
   if (typeof lefthookPackage.bin?.lefthook !== 'string') return
